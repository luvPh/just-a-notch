import SwiftUI

enum IslandTheme {
    static let cream = Color.black
    static let ink = Color.white
    static let cobalt = Color(red: 0.35, green: 0.34, blue: 0.93)
    static let card = Color.white.opacity(0.08)
    static let mutedInk = Color.white.opacity(0.62)
}
